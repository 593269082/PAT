#include<iostream>
#include<algorithm>
#include<string>
#include<queue>
#include<math>
using namespace std;

int main() {
	int N;
	cin >> N;
	queue<int> Q;
	int currentN;
	int Max = 2;
	int sqr = (int)sqrt(1.0 * N);
	currentN = N;
	for (int i = 2; i <= sqr; i++) {
		for (int j = 2; j <= sqr; j++) {

		}
	}

	return 0;
}